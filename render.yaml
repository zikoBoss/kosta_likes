services:
  - type: web
    name: zakaria-outfit
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: MAIN_KEY
        value: CTX-TEAM